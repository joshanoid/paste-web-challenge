import * as React from 'react'

import styles from './main.module.css'

export const Main = () => (
    <div className={styles.app}>
        <header className={styles['app-header']}>Test header</header>
    </div>
)
